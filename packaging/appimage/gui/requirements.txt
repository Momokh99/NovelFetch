# The built wheel is copied into this directory (as novelfetch.whl) by the
# release workflow before python-appimage runs.  Installing the [gui] extra
# pulls Kivy, KivyMD, and the RTL text shapers — all the GUI runtime deps.
novelfetch.whl[gui]
